var Partner = function(idSupplier, name, phone, email, address, userId) {
    this.Id = idSupplier,
    this.Name = name,
    this.Phone = phone,
    this.Email = email,
    this.Address = address,
    this.UserId = userId
}

module.exports = Partner;